---
description: "A sales call just ended: turn its transcript into the full follow-up (CRM, tasks, email draft, reminder, coaching)"
---

The user typed `/cierre`. Invoke the `cierre-de-llamada` skill via the Skill tool, forwarding any arguments the user provided after the slash command. If `cierre-de-llamada` is not in the available-skills list, tell the user the skill isn't installed and suggest running `bash ~/plugins/codex-brain-starter/bootstrap.sh` to install it.
